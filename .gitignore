# ── Environment variables (NEVER commit these) ──
.env
.env.local
.env.production
js/config.js

# ── Node modules ──
node_modules/

# ── OS files ──
.DS_Store
Thumbs.db

# ── Logs ──
*.log
npm-debug.log*

# ── Build artifacts ──
dist/
build/
